{
  "name": "mrivals-stat-viewer-workspace",
  "private": true,
  "version": "1.0.0",
  "description": "Workspace scripts for frontend and backend",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview",
    "backend": "dotnet run --project .\\backend\\Backend.csproj",
    "install:all": "npm --prefix frontend install && dotnet restore .\\MarvelRivalsTracker.sln"
  }
}
